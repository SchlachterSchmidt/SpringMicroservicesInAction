server:
  port: 8888
spring:
  cloud:
    config:
      server:
        encrypt.enabled: false
        git:
          uri: https://github.com/SchlachterSchmidt/SpringMicroservicesInAction/
          searchPaths: config-repo/licensingservice,config-repo/organizationservice,config-repo/gatewayservice
